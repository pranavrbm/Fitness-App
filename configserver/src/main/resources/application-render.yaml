spring:
    application:
        name: config-server
    profiles:
        active: native
    cloud:
        config:
            server:
                native:
                    search-location: classpath:/config

# Disable config server for Render (remove the config import)
server:
    port: ${PORT:8888}
